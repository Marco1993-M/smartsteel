import OsSectionPlaceholder from "../../../components/os/OsSectionPlaceholder"

export default function AnalyticsOsPage() {
  return (
    <OsSectionPlaceholder
      title="Analytics workspace"
      description="This section will consolidate sales, quoting, pipeline, product, and operating performance so the team can make decisions from one trusted reporting surface."
      nextStep="Tie CRM metrics, quote momentum, product-line mix, and operating throughput into one dashboard once the foundational workspaces are established."
    />
  )
}
