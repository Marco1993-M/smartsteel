export const metadata = {
  title: "AFGRI Atlas Sales Portal",
  description: "AFGRI sales access to approved Atlas products, configurations and quote requests.",
  robots: { index: false, follow: false },
}

export default function PartnerLayout({ children }) {
  return children
}
